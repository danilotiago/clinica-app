import { InputStringValidator } from './input-string-validator';

describe('InputStringValidator', () => {
  it('should create an instance', () => {
    expect(new InputStringValidator()).toBeTruthy();
  });
});
